package com.example.cuidapet.ui.screens.pets.models.enums

enum class DosageType {
    SINGLE,       // Dose única
    SERIES,       // Série de doses (ex: 3 doses)
    YEARLY,       // Vacina anual
}